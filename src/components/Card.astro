---
interface Props{
    img:string,
    spanish:string,
    english:string,
    latin:string,
    url:string
}
const {img, spanish, latin, english, url} = Astro.props
---
<a href={url}>
    <div class="flex flex-row rounded-lg bg-zinc-600 min-h-full">
        <div class="avatar">
            <div class="w-48 rounded-lg">
              <img class="object-cover min-h-full w-full " src={img} alt={latin}/>
            </div>
          </div>

        <div class="card-body text-zinc-300 justify-center">
            <h1 class="text-2xl">{spanish}</h1>
            <h2 class="italic">{latin}</h2>
            <h2>{english}</h2>
        </div>
    </div>
</a>